# REST-API-Books-Registry

  - This is a REST API for book registry with a authentification system, using JWT Token, the mdp is hashed with "extra salt"
  - The data is store by using mongoDB for his flexibility
